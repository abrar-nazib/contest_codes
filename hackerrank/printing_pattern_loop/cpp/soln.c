// #include <bits/stdc++.h>
// using namespace std;
#include <stdio.h>
#include <math.h>
#include <stdlib.h>

#define _DEBUG 1

int main()
{
#ifdef _DEBUG
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    int n;
    scanf("%d", &n);
    int range = (2 * n) - 1;
    for (int i = 0; i < range; i++)
    {
        return;
    }
    return 0;
}