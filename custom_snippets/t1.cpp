#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>
#include <algorithm>
#include <cmath>
#include <cstring>
#include <string>
#include <queue>
#include <stack>
#include <deque>
#include <list>
#include <bitset>
#include <utility>
#include <functional>
// #include <cstdio> // Optional, since you can use <iostream> instead

using namespace std;

// Shortcut for commonly used data types
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pii;

// Constants
const int MOD = 1e9 + 7;
const int INF = 1e9;

// Shortcuts for iteration
#define loop(i, n) for (int i = 0; i < (n); ++i)
#define rloop(i, n) for (int i = (n)-1; i >= 0; --i)
#define rep(i, a, b) for (int i = (a); i <= (b); ++i)
#define rrep(i, a, b) for (int i = (b); i >= (a); --i)

// Shortcut for input/output
#define endl '\n'

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    // Your code here
    cout << "Hello" << endl;
    return 0;
}
